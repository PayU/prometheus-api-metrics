'use strict';

const expect = require('chai').expect;
const supertest = require('supertest');

describe.skip('when using express framework', function () {
    describe('when start up', function () {
        it('should populate default metrics', function (done) {

        });
        describe('when calling a GET endpoint', function () {
            it('should add it to the histogram', function (done) {

            });
        });
        describe('when calling a POST endpoint', function () {
            it('should add it to the histogram', function (done) {

            });
        });
        describe('when getting an error', function () {
            it('should add it to the histogram', function (done) {

            });
        });
    });
});
